/*
package dataModels contains a few data models that are used by both the amqp package and
the middleware package.

These models must be stored in their own package to avoid circular imports, and aliases
to them are made available in the amqp package.
*/
package datamodels
